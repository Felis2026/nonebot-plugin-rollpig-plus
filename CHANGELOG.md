# 更新日志

## v0.11.0

### 预约烤猪

- 自己未抽猪时不再能烤群友
- 指定目标未抽猪时建立当前群的预约烤猪；第一位用户成为固定主厨并支付资源，后续群友免费添柴，单场最多 12 人且同一参与者不会重复加入。
- 预约结果先固定为 `prepared`，真正调用 QQ 发送前再进入不可自动重领的 `sending`；接口响应丢失时可按同一领取凭证安全重试，避免重复发送。
- 仅持有未完成预约的 Owner Bot 每 60 秒最多发起一次合并领取请求；资源缺失与群功能关闭会在本机退避，资源同步或群内有效指令可提前唤醒。
- 预约反噬继续把主厨和原目标写入既有事件字段，实际被反噬者写入结果与事件快照，避免改变旧日报统计语义。
- 新插件连接旧 Cloud 时只对预约功能降级为原提示，旧插件连接新 Cloud （0.3.0）的所有既有接口与语义保持不变。

## v0.10.0

### 共享烤猪文案

- 默认随小猪资源同步获取审核后的共享烤猪文案，即使没有配置 AI Key，也能使用持续更新的烤猪与烤群友模板。
- 共享快照合并进唯一的本地 `roast_library.json`；新增 `roast_library_sources.json` 记录本地与共享来源，不重复保存正文。
- 首次升级会把已有正文全部保守标记为本地来源，并逐条保留因引号或空白差异形成的旧正文；云端更新、撤回或关闭同步只能删除纯共享内容，不覆盖实例已有 AI 文案。
- 新增 `rollpig_roast_library_manifest_url`：未填写时使用官方地址，自定义 URL 可替换来源，显式 `""` / `null` 可关闭并移除纯共享正文。
- 同步比较 URL、版本、SHA256 与应用状态，并加入大小预算、结构校验、临时 staging、原子落盘、损坏文件隔离和失败回退。
- 共享同步接入启动、定时检查与 `同步小猪资源`；图片资源与共享文案任一来源失败都不会阻断另一来源，也不会影响现有本地内容。

### AI 文案池

- 每个“原始猪 × 熟食 × 场景”的本地 AI 文案软目标由 3 条提高到 5 条，达到目标后不再主动生成，但不会裁剪已有超额文案。
- 共享文案不计入本地 5 条额度；本地池与纯共享池同时可用时先等概率选池，避免共享库规模增长后淹没实例自有内容。

### EX 等级差分

- 新增 `pig_ex_variants.json` 与 manifest 差分图片协议，支持 EX Lv.1～5 稀疏配置；图片与两类文案可独立继承较低等级差分。
- “今日小猪”和“小猪图鉴”按用户已有 `copies` 显示同一只猪的等级立绘；差分可覆盖描述与分析文案，但不能改变 ID、名称或玩法规则。
- 跨过资源真实提供的差分等级时，按图片、文案或两者同时变化选择对应的完整成长文案，不再叠加生硬的固定解锁提示；重复查看、今日烤猪、烤群友、昨日、本周和日报继续保持原有语义。
- 差分同步沿用大小、SHA256、路径预算、staging 与原子激活保护；坏差分不会覆盖当前资源，运行时渲染失败会重试基础图片与文案。
- 兼容旧客户端先同步新资源的场景：升级后即使资源版本号相同，也会自动补齐旧客户端未下载的差分文件。
- 不新增依赖、配置或数据迁移；未发布差分的资源包和已有实例行为保持不变。

## v0.9.0

### 图片版图鉴渲染

- 图片版小猪图鉴改为纯 Pillow 绘制，保留分页、EX Lv.、NEW / MAX 标记、统计信息和结果缓存。
- 重做半透明毛玻璃卡片、立体等级胶囊和顶部统计布局，并优化 Windows、Linux、Docker / WSL2 下的中文字体加载。
- 图鉴绘制移入工作线程并保留有界并发，避免阻塞 NoneBot 事件循环或在突发请求下产生无上限的内存峰值。

### 依赖与兼容

- 移除图鉴 HTML 模板、Playwright 页面池、截图后处理及 `nonebot-plugin-htmlrender` 依赖。
- 保持图鉴命令、业务快照、用户数据和资源目录兼容，现有部署升级后不需要迁移数据。

### 卡片缓存与 GIF 内存边界

- 固定文案 PNG/GIF 卡片共用 64 MiB 磁盘 LRU，只按总字节淘汰且跨重启保留；资源图内容或文案变化会自动避开旧成品。
- 烤猪等动态文案不缓存最终成品，只复用最多 8 条、24 MiB 的 GIF 头像帧内存 LRU，避免随机文案持续写盘。
- GIF 首次生成改为逐帧合成、立即量化并最多双并发执行，避免同时常驻整批 800×800 RGB 帧或让过多冷请求叠加峰值。
- 较长动画会在完整周期内均匀收敛到最多 60 帧并合并原始时长；超过 1600 万像素帧预算或 600 源帧绝对上限时安全降级为静态首帧。
- 图鉴和本周小猪继续只使用 GIF 首帧生成静态 PNG，不进入普通卡片的动态源帧缓存。

## v0.8.2

### 多私有资源包

- 新增 `rollpig_private_resource_manifests`，支持按顺序叠加多个私有 overlay 资源包。
- 旧版 `rollpig_private_resource_manifest_url` / `rollpig_private_resource_token` 继续兼容，会作为 legacy overlay 追加加载。
- 私有包支持填写本地 `manifest.json` 路径或 `file://` URL，方便用户在本机维护自己的小猪资源包。
- 多私有包使用独立缓存目录，单个私有包同步失败时不会影响公有资源或其它私有包。

### 资源包扩展

- 可同时启用公开资源包、官方 GIF 包、PJsk 私有包和用户自建私有包。
- 官方 GIF 动态小猪 overlay 不走用户配置，会随云端资源同步固定叠加。
- 私有 overlay 继续要求新增猪与已有 ID 不冲突；覆盖已有猪需要使用 `pig_overrides.json` 明确声明。

## v0.8.1

### 每日总结与分群控制

- 每日总结默认改为关闭，避免新部署实例在管理员未确认前主动向群聊推送定时消息。
- 新增 `小猪日报` / `小猪日报 状态` / `小猪日报 开启` / `小猪日报 关闭` 命令，支持群主或管理员控制本群，SUPERUSER 可追加群号控制其他群。
- `rollpig_daily_summary_enabled` 调整为未被单群命令或外部控制器覆盖时的默认值；需要全局默认开启时可显式设为 `true`。
- 新增日报控制器接口；公开版无外部控制器时写入插件本地数据，宿主项目接入外部控制器时可保持单一状态源。
- 日报定时任务先筛选已开启日报的群，再生成日报与次日保护名单，避免关闭日报的群被定时任务产生额外副作用。

## v0.8.0

### 普通卡片渲染

- `今日小猪`、`昨日小猪`、`今日烤猪`、`烤群友` 等普通卡片改为 Pillow 渲染，降低普通图片输出对 Chromium / Playwright 的依赖。
- 内置 Source Han Sans SC Medium 作为默认 CJK 字体，并保留 `rollpig_card_font_path` 供用户自行指定字体。
- 内置 Google Noto Emoji 32px ZIP，普通卡片可离线渲染彩色 Emoji，减少 Docker / Linux 环境缺字体导致的方块字或黑白 Emoji。
- 普通卡片保持 800×800 输出，头像资源统一规整到 240×240 区域；图片版小猪图鉴仍沿用 HTML/CSS 渲染。

### GIF 动态资源

- 普通卡片支持 GIF 小猪资源，`今日小猪`、`烤猪`、`烤群友` 等图片输出会随资源自动生成 GIF。
- 资源图片支持 `.png` / `.gif`，同 ID 同时存在时优先使用 GIF。
- 图片版小猪图鉴固定取 GIF 首帧缩略图，保持静态陈列。
- GIF 输出使用全帧采样调色板，兼顾文字稳定和动态头像色彩保留。

### 内置资源更新

- 将内置公共资源同步到 `rollpig-resources/rollpig` 的 `2026-06-28.1` 版本，默认小猪数量更新至 165 只。
- 新增 `猪水群`、`猪诞树`、`猪龙鱼公爵`、`肥猪`、`猪健身`、`狮子猪`、`猪泥好运`、`护士小猪`、`油画猪`、`猪兔同笼`、`猪打滚`、`震撼美味`、`小心猪滑`、`抽搐猪`、`飞碟猪`、`猪意味`。
- 同步最新 `pig_rules.json`，保持熟食、特殊形态、烤猪排除等规则与公共资源包一致。

### AI 烤猪

- 默认模型切换为 `deepseek-v4-flash` 非思考模式。
- 自动兼容旧 `deepseek-chat` / `deepseek-reasoner` 配置，避免 DeepSeek V4 迁移期旧配置直接失效。

## v0.7.4

### 稳定性修复

- PigHub 图库刷新增加 single-flight、失败冷却与旧缓存回退，避免并发缓存击穿。
- `roast_library.json` 写入改为异步锁、线程落盘与临时文件原子替换，降低事件循环阻塞和 JSON 损坏风险。
- 启动期资源同步后台任务纳入 shutdown 生命周期，避免关闭期仍在改动资源缓存目录。

### 并发与数据边界

- 图鉴缓存增加同 key single-flight，避免多个相同请求重复占用 Chromium 渲染资源。
- 本地数据读取接口返回副本，避免命令层误改内部 list / dict 后绕过锁与原子保存。
- 旧烤群友 `check_roast_usage()` / `update_roast_usage()` 标记为 deprecated，后续新流程统一使用原子扣减接口。

## v0.7.3

### 独立发布准备

- 将 Felis-dev整理为独立仓库与独立包名 `nonebot-plugin-rollpig-plus`。
- Python 模块名调整为 `nonebot_plugin_rollpig_plus`，避免与原作包名直接冲突。
- 更新插件元数据、README、CHANGELOG、预览图与配置示例，补齐面向 GitHub / PyPI / NoneBot 商店的发布说明。

### 内置资源更新

- 将公共资源包内置进插件默认资源，默认小猪数量更新至 149 只。
- 同步最新 `pig_rules.json`，内置熟食类、售罄类等资源规则。
- 保持私有资源 overlay 默认关闭，仍可通过配置手动启用。

## v0.7.2

### 稳定性修复

- 加固本地 `pig_data.json` 读取与写入流程，避免坏 JSON 被静默覆盖。
- 加固云端资源同步，改为流式下载、单文件大小上限与临时目录激活。
- 加强资源包校验，下载失败时保留旧缓存或回退到内置资源。
- 修复今日小猪检查与保存之间的并发竞态，降低重复抽取风险。

### 性能与 AI 边界

- 新增 HTML/Chromium 渲染总并发预算，避免多类生图任务互相打爆。
- 图片版小猪图鉴引入常驻页面池与缓存上限。
- AI 烤猪新增并发、token、输出字符数与超时边界。
- 默认 AI 并发调整为 4，兼顾可用性与请求堆积风险。

## v0.7.1

### PigHub 兼容性修复

- 修复 PigHub 旧接口失效导致的 `找猪` / `搜猪` 不可用问题。
- 新增新旧 PigHub 接口兼容。
- 优化搜索失败时的异常兜底。

## v0.7.0

### 图片版小猪图鉴

- 新增 `小猪图鉴` 图片命令。
- 使用 HTML/CSS 渲染图鉴内容层，并与底图合成。
- 新增图鉴聚合快照、EX Lv. 展示、NEW / MAX 标记。

### 配置与性能

- 新增 `rollpig_config.json` / `config/rollpig.json` 非敏感配置文件支持。
- 保留 `.env` / NoneBot 配置对敏感项和部署覆盖项的优先级。
- 新增图鉴渲染缓存、渲染超时与缩放配置。

### 烤群友充能

- 新增普通烤群友充能存储。
- 支持多 Bot 状态同步时共享充能状态。

## v0.6.2

### 私有资源 overlay

- 支持公有资源包之上的私有资源 overlay。
- 支持私有资源 manifest 与 token 配置。

### 特殊形态

- 新增更多特殊形态兼容。
- 熟食类、吃掉了、人类、售罄等形态参与烤猪拦截。

## v0.6.1

### 云端资源同步

- 新增云端小猪资源包同步。
- 支持从 `manifest.json` 拉取 `pig.json`、`pig_rules.json` 与图片资源。
- 新增超管命令 `同步小猪资源` / `刷新小猪图鉴`。

## v0.6.0

### 新功能

- 新增 EX Lv. 成长机制。
- 新增猪圈进度摘要。
- 新增烤群友玩法与更多特殊文案。

### 云端同步

- 新增 rollpig-cloud 存储后端。
- 支持今日小猪、图鉴成长状态、烤群友充能等状态跨 Bot 同步。

## 更早版本

更早版本主要继承自原作插件与早期 Felis 分支，包含基础今日小猪、随机小猪、找猪、本周小猪、烤猪与资源扩展等功能。
